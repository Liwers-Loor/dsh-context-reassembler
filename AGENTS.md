# AGENTS.md — dsh-context-reassembler 仓库规则

本文件是本仓库的仓库级规则，对本仓库内的所有开发工作（包括 AI 编程代理）一律生效。

## 规则清单

1. 在本项目开发过程中，禁止启用 GitHub Actions。

2. 本项目中按照「模块」、「组件」、「功能」的方式称呼开发内容的规模。

3. 创建分支时，按照模块、组件、功能的顺序进行分支命名（以下占位符按第 10 条约定以单词首字母大写书写）：
   - 模块开发分支：`sub/<Module-Name>`
   - 组件开发分支：`<Module-Name>/<Component-Name>`
   - 功能开发分支：`<Component-Name>/<Function-Name>`

4. 阶段性开发任务完成时，要进行 Draft PR、设置回滚点，同时增量撰写工作日志。

5. 本项目中的工作日志，在每次增量撰写完后，直接 git push 即可，不影响 PR 和回滚点。

6. 本项目中的工作日志，允许跨日期进行撰写，允许录入旧日志，但不允许修改已提交日志的内容。

7. 本项目中的工作日志，按照年月日形式命名；增量撰写时，需精确到时分，即按年月日时分的形式，标注工作日志增量撰写时间，无需精确到秒数。
   示例：日志文件 `2026-08-16.md`，增量撰写时间标注 `2026-08-16 14:30`。

8. 本项目的开发内容要进行修改、维修、重构、升级时，在第 3 条的分支命名后，加下划线和对应操作。如维修某组件时，命名为 `<Module-Name>/<Component-Name>_Repair`。

9. 本项目的开发内容要进行合并、拆分、重组时，分支命名需将对应下级内容全部列出，内容名之间用下划线相连，不可进行缩写。如某组件下属两个功能进行合并时，命名为 `<Component-Name>/<Function-Name1>_<Function-Name2>_Combine`。

10. 本项目中所有分支的命名都是大小写敏感、单词首字母大写。

11. 定义本项目的开发内容操作中英文对照如下：

| 中文操作 | 英文 |
| --- | --- |
| 修改 | Alter |
| 维修 | Repair |
| 重构 | Revamp |
| 升级 | Upgrade |
| 合并 | Combine |
| 拆分 | Deconst |
| 重组 | Regroup |
